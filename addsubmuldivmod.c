#include<stdio.h>

int main()
{
    int a=2,b=4,add,sub,mul,div,mod;
     
     add=a+b;
     sub=b-a;
     mul=a*b;
     div=a/b;
     mod=a%b;
     
     printf("\naddtion of 2 and 4 is %d",add);
     printf("\nsubstitute of 4 and 2 is %d",sub);
     printf("\nmultiplication of 2 and 4 is %d",mul);
     printf("\ndivision of 2 and 4 is %d",div);
     printf("\nmod of 2 and 4 is %d",mod);
}

